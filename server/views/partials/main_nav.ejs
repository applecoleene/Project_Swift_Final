<!--Main Navigation-->
<nav class="navbar navbar-expand-lg navbar-light" id="navHeader">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">
            <img src="/Assets/images/logo_blue.png" alt="swift logo" width="110" height="40"
                class="d-inline-block align-text-top">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                <% if (title == "Home") { %>
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/home"><i class="fas fa-home"></i> Home </a>
                    <% } else { %>
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="/home"><i class="fas fa-home"></i> Home </a>
                    <% } %>
                </li>
                <!--
                <% if (title == "Survey Table") { %>
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/survey-list"><i class="fas fa-poll-h"></i>
                        Survey</a>
                    <% } else { %>
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="/survey-list"><i class="fas fa-poll-h"></i> Survey</a>
                    <% } %>
                </li>
                -->

                <% if(typeof displayName == 'string' && displayName) { %>
                <% if (title == "Surveys") { %>
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/survey-list"><i class="far fa-list-alt"></i>
                        Survey
                        Table
                    </a>
                    <% } else { %>
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="/survey-list"><i class="far fa-list-alt"></i> Survey
                        Table
                    </a>
                    <% } %>
                </li>
                <!--<li class="navbar-text"><i class="fas fa-user"></i> Welcome, <%= displayName %></li>-->
                <li class="nav-item"><a href="/logout" class="nav-link">
                        <i class="fas fa-lg fa-sign-out-alt"></i> Logout</a></li>
                <% } else { %>
                <li class="nav-item"><a href="/login" class="nav-link">
                        <i class="fas fa-lg fa-sign-in-alt"></i> Login</a></li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>